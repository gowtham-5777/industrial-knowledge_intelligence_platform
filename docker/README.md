# Docker — Industrial Brain AI (Milestone 1.9)

One-command local stack from the **repository root**:

```bash
cp .env.example .env
docker compose up --build
```

See the root [README.md](../README.md) for ports, seed users, and optional corpus mounts.
